[test]
# Use `bun run test` for the complete suite: scripts/test.ts isolates every file
# (including workspaces) in its own process, environment and data directory.
# `bun test path/to/file.test.ts` remains useful for a single-file check.
root = "./src"
